# Secrets: bot token + channel ids. Lives here now; ~/.config/claude-slack/config.json
# is a symlink to it so the notify skill keeps working.
config.json
# Machine-local PATH for schedulers, written by install.sh.
path.env
# Runtime state, logs, cursors, user cache — machine-local, some has Slack data.
state/
# Bare clones of the org's PRIVATE repos — never push these.
repos/
# Throwaway PR/issue checkouts.
wt/
# Local MCP binary.
ts-tools
